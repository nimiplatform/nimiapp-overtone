# Nimi Overtone

整理音乐创作项目、歌词与音频候选，比较和管理创作版本。

## 使用方式

1. 开始新创作，手动填写歌曲简报与歌词，或使用已配置的文字生成功能辅助创作。
2. 配置音乐生成能力后，根据简报、歌词与风格方向生成音频候选；修改输入可再次生成新版本。
3. 播放候选、进行 A/B 比较，标记喜欢的版本。应用在本机自动尝试保存当前项目草稿；开始替代项目或放弃项目会移除原有草稿。

当前版本尚不支持音频续写、混音、参考音频与发布。生成能力不可用时，已有草稿仍会保留。

## 运行条件

在 Nimi 的应用列表中安装或导入此应用，再点击启动。应用由 Nimi 启动和管理；运行状态、App Access 和所需能力以 Nimi 的实际状态为准。

使用 AI 功能前，在 Nimi 中完成此应用的 AI 配置。缺少所需模型或能力时，相关功能会报告不可用，不会生成替代结果。

## 数据与支持

应用在本机保存可恢复的项目草稿（简报、歌词和音频候选引用），以及语言、主题和音量偏好，通常占用几 KiB 至几 MiB。它们保存在 Electron 用户数据目录的 Local Storage 中；从 Nimi 卸载应用后，这部分数据仍会保留。

音频字节不保存在草稿中，候选引用指向 Runtime 管理的音频资产。重新打开草稿时需要读取这些资产；资产不可用时，候选信息仍保留，但无法播放。草稿受本机浏览器存储配额等条件限制，不是跨设备备份。

[使用与开发说明](https://github.com/nimiplatform/nimiapp-overtone) · [反馈问题](https://github.com/nimiplatform/nimiapp-overtone/issues)
